CREATE TABLE BOOK (
                      ID BIGINT PRIMARY KEY AUTO_INCREMENT,
                      TITLE VARCHAR(255),
                      AUTHOR VARCHAR(255),
                      PRICE DOUBLE,
                      QUANTITY INTEGER
);
